# Enable Powerlevel10k instant prompt. Should stay close to the top of ~/.zshrc.
# Initialization code that may require console input (password prompts, [y/n]
# confirmations, etc.) must go above this block; everything else may go below.
if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

###########
# ENVIRON #
###########
export PATH="$PATH:$HOME/.local/bin"
export __GL_THREADED_OPTIMIZATIONS=1
export __GL_SYNC_TO_VBLANK=0
export __GL_YIELD=USLEEP

#############
#  GENERAL  #
#############

export ZSH="$HOME/.oh-my-zsh"

#zstyle ':omz:update' mode reminder  # just remind me to update when it's time

#zstyle ':omz:update' frequency 13   # how often to auto-update (in days).

# See https://github.com/ohmyzsh/ohmyzsh/wiki/Themes
ZSH_THEME="powerlevel10k/powerlevel10k"

CASE_SENSITIVE="false"

# Uncomment the following line to use hyphen-insensitive completion.
# Case-sensitive completion must be off. _ and - will be interchangeable.
# HYPHEN_INSENSITIVE="true"

# Uncomment the following line if pasting URLs and other text is messed up.
# DISABLE_MAGIC_FUNCTIONS="true"

DISABLE_LS_COLORS="false"

DISABLE_AUTO_TITLE="false"

ENABLE_CORRECTION="true"

COMPLETION_WAITING_DOTS="false"

###########
# HISTORY #
###########
export HISTFILE=~/.zsh_history
export HISTSIZE=10000
export SAVEHIST=10000
setopt appendhistory
setopt sharehistory
setopt hist_ignore_space
setopt hist_ignore_all_dups
setopt hist_save_no_dups
setopt hist_ignore_dups
setopt hist_find_no_dups


#############
#  PLUGINS  #
#############
plugins=(
zsh-autosuggestions
zsh-syntax-highlighting
git
zsh-bat
you-should-use
)

source $ZSH/oh-my-zsh.sh


########################
#  User configuration  #
########################
export LANG=en_US.UTF-8

# Preferred editor for local and remote sessions
if [[ -n $SSH_CONNECTION ]]; then
  export EDITOR='vim'
else
  export EDITOR='nvim'
fi

# Man page highlighting with nvim
# export MANPAGER="nvim +Man!"

# Man page highlighting with bat
export BAT_THEME="Solarized (dark)"
export MANPAGER="sh -c 'col -bx | bat -l man -p'"
export MANROFFOPT="-c"


#################
# Powerlevel10k #
#################
source ~/.oh-my-zsh/custom/themes/powerlevel10k/powerlevel10k.zsh-theme

# To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh


######################
#      ALIASES       #
######################
alias :q="exit"
alias py="python"
alias vim="nvim"
alias miku="neofetch"
alias ls='eza --icons --color=always --group-directories-first'
alias ll='eza -alF --icons --color=always --group-directories-first'
alias la='eza -a --icons --color=always --group-directories-first'
alias l='eza -F --icons --color=always --group-directories-first'
alias l.='eza -a | egrep "^\."'
alias mk="musikcube"
alias tmt="./.local/bin/timecanvas/TimeCanvas"
alias dr="dotnet run"
alias dpl="dotnet publish -r linux-x64 --sc"
alias dpw="dotnet publish -r win-x64 --sc"
alias wall="swww img"
alias zshc="nvim .zshrc"
alias neoc="nvim .config/neofetch/config.conf"
alias jr="java"
alias jc="javac"
alias sddmc="nvim /usr/share/sddm/themes/eucalyptus-drop/theme.conf" 
alias hyprc="nvim .config/hypr/general.conf"
alias bindc="nvim .config/hypr/keybinds.conf"
alias startupc="nvim .config/hypr/execs.conf"
alias barc="nvim .config/waybar/config.jsonc"
alias quote="fortune | cowsay | lolcat"
alias cat="lolcat"
alias virtual="source .venv/bin/activate"
alias anime="cd anime && npm run dev"
alias waifu="yazi .waifus"
alias picture="yazi ~/Pictures/Wallpapers"
alias podc='bluetoothctl connect F4:6F:C6:F6:3D:43'
alias podd='bluetoothctl disconnect F4:6F:C6:F6:3D:43'
alias idf='. $HOME/esp-idf/export.sh'
alias podb='bluetoothctl info'
alias bye="poweroff"
alias goodbye ="poweroff"
alias goodnight ="poweroff"
alias gn="poweroff"
alias gb="poweroff"
alias taovoucher="python generate_voucher.py"
alias g4f='docker run -p 8080:8080 -p 1337:1337 -p 7900:7900 --shm-size="2g" -v ${PWD}/har_and_cookies:/app/har_and_cookies -v $HOME/Pictures/generated_images:/app/generated_images hlohaus789/g4f:latest'
eval "$(zoxide init zsh)"
alias uzipa="for i in *.zip; do unzip $i; done"

# pnpm
export PNPM_HOME="/home/phong/.local/share/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac
# pnpm end
